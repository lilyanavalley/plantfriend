[target.riscv32imc-esp-espidf]
linker = "ldproxy"
# Required by embuild / esp-idf-svc
runner = "espflash flash --monitor"
rustflags = [
    "--cfg", "espidf_time64",
]

[unstable]
build-std = ["std", "panic_abort"]
build-std-features = ["panic_immediate_abort"]
